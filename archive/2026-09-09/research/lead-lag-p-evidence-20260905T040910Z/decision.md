# P：跨资产领先—滞后，有界原始证据评估

结论：**1m 经济假说有足够的一般依据支持有条件的新样本有限证伪；当前为 `BLOCKED_EXECUTION_EVIDENCE`，不是整个方向的 `NO_GO`，也不是可执行协议或合格策略。** 此标签仅用于本次审计，不是项目或 native 的运行状态。初步“5m 不适配”判断只针对该实现尺度，不应升级成经济否定。

已实际读到 Kurihara & Matsumoto 的期刊 HTML 正文（方法、结果、结论、脚注；未下载 PDF，未核图表底层数据）。研究使用 Binance 1m close/成交笔数；低成交小币多有约 1m 延迟，2–3m 衰减，主流币多同步。Bull 全段低成交排名选资产，而该段包含末周 OOS；资产池选择可能污染该周，不能把其他独立时期一并判无效。交易模型含自身和 BTC 滞后收益，费用假设 0.02%/边。§3.7 的 x[t−1]→ω[t]、ω[t]×r[t] 覆盖前一 close 至后一 close；**执行边界价格可得性未验证，公式本身并不证明 lookahead**。交易式未呈现价差、滑点和发布/成交延迟。[期刊正文，§3.1–3.7、§4.1/4.4/4.6、脚注7](https://link.springer.com/article/10.1007/s10690-026-09589-z)

第二篇候选 [Bitcoin, Ethereum, and the Ambiguity of Price Discovery](https://www.mdpi.com/1911-8074/19/9/678) 仅读到搜索摘录；正文与官方 `/htm` 备用入口各失败一次。未把摘录当正文，不将其数值用于结论；按边界停止检索，未扩大论文数量。

可证伪预测是：在参考资产 L 的一分钟变化和目标 A 的同期变化均已闭合、实际到达之后，L 的变化对 A **首次可成交之后约下一分钟**的收益仍提供自身历史无法解释的同向增量，且交易净收益能覆盖摩擦。信息来源是跨资产调整速度差；不是 N 的自身冲击延续，不是 BTC/ETH 双动量轮换，也不是 breadth。BTC 是这篇证据支持的初始参考假设，不能宣称普遍 leader；不按新测试集挑 leader、目标或 lag。无需双腿交易，也不需要持有参考资产。

忠实的首笔成交应满足 `T_exec >= max(T_close_L, T_close_A, T_arrival_L, T_arrival_A) + decision/order latency`。1m 下的下一根 open 是 native 的模拟边界，不保证现实成交。5m 汇总会混合已完成与尚未完成的调整；末段冲击仍可能留下残余，所以不能证明所有 5m 版本无效。持仓尺度应先围绕一分钟的调整，而非凭空延长到 30m/1h 去回避成本；这里没有冻结入场阈值或最大持仓参数。

费用可作临界计算，不能冒充实际测量。OKX 官方页面的 Lv1 示例为 taker 0.05%/边，并说明具体适用性须按账户/产品确认。[官方费用方法，2026-08-26 更新](https://www.okx.com/en-ae/help/how-to-calculate-the-contract-transaction-fee) 本次未访问账户，实际 Profile 费率、目标价差、滑点和 funding 均为 `UNKNOWN`。以此示例计算，等量多头两边手续费的保本价格涨幅为 `(1+0.0005)/(1-0.0005)-1 ≈ 10.005 bp`，尚未计价差/冲击和净 funding；每边再损失 1 bp，往返约再加 2 bp。不能以 maker 低费率替代 taker 成交来制造边际；短持仓跨结算也不能默认 funding=0。未知毛收益与未知摩擦不能推出“已经济失败”，但目前无法确认有限实验的真实成本余量。

当前代码与最小能力判断：

- 新工作树 `/Users/shenjianpeng/.codex/worktrees/1ba8/freqtrade-lab` 干净、detached HEAD；本地 HEAD 与只读 `git ls-remote origin refs/heads/main` 均为 `0ace04b7c10ea35fb8ce6f25e043ac78be87c19e`。
- native 指定源码 HEAD 为 `52bc96f4480b1a0da6a9b455bd00b17fbb6786a5`。`strategy/interface.py:853` 已有非交易参考资产 `informative_pairs`，`:1077` 汇集输入；`strategy/strategy_helper.py:45` 同周期按时间合并，较快 informative 不能直接并入较慢主周期。`optimize/backtesting.py:555` 起将信号后移一根，`:1152` 使用当根 open 作为默认入场价。全部仅静态阅读。
- 项目 `lab/bounded_strategy.py:27,45,66–67` 限制 512 lookback、三个 populate、5m/1d 与 OHLCV 源列；`lab/bounded_research.py:177`、`lab/codex_generation.py:509`、`scripts/run_freqtrade_backtest.py:584,847,911` 也有周期/步长契约。不是改一行 timeframe 即可。
- 最小复用扩展为：已有 Profile/producer/consumer 允许 1m；绑定一个明确市场类型的 1m 非交易参考资产；source provenance/hash/窗口/缺失处理同时覆盖两资产；用原生 DataProvider 和同周期 informative 接线，只暴露受限参考列；缺失或陈旧参考值不得无限前填；验证最后未闭合数据拒绝、未来参考变更不影响过去信号、交易与参考窗口共同隔离。沿用六表与既有 JSON 证据，不新增 runner、服务或平台。
- 若采用严格一分钟到期退出，必须有原生受限退出路径，允许亏损退出且避免 entry/exit 冲突；普通正 ROI 不保证到时退出（`strategy/interface.py:1712` 比较当前利润是否高于 ROI）。目前三个 populate 不能任意访问 trade age，不应伪装成已有固定持仓能力。
- 上述 1m+单参考+退出/契约贯通，静态粗估 **2–4 个主动工作日**，含针对性因果/失败检查和原生入口核验；假设沿用现有 producer 与离线数据格式、不引入训练平台。计算时间、数据等待时间均 `UNKNOWN`。这是研究估计，未实施、未做完整工程盘点。

报价缺口应按用途处理：1m OHLC 可以做成本保守的**否证筛查**，负面结果对那份冻结协议有价值；成功只能说明值得进一步验证。正向经济判断至少要证明首笔打印价近似可成交价，例如有时间一致的 best bid/ask 与小额成交容量证据，或有独立依据的保守摩擦/延迟上界。无需先建完整盘口平台、maker 队列或毫秒策略；目前也没有证据证明必须超低延迟才成立。目标恰是低成交资产，不能用 volume>0 或完整 K 线替代这个检查。历史报价可得性、目标在 OKX 的合约覆盖和有效参考市场类型均未核。若只剩大量获取盘口或工程平台才能检验，个人项目应暂停这一支，而不是先开发。

基线与唯一增量在原理上成立：固定目标、时间、成本、执行/退出、校准方式，基线仅用目标上一分钟收益预测下一可成交后的收益，增量只加入一个固定参考资产上一分钟收益；模型结构/参数选择预算保持一致。用 buy-and-hold 单独当对照不足以识别跨资产增量。此为研究设计轮廓，**未冻结估计法、目标、窗口、阈值或 native 协议**，也不把论文模型直接塞进当前 bounded 模板。

下一门仅是**输入/成交证据可得性核验，不是立即投入 2–4 天开发**。建议先做一项不超过约一小时的便宜检查：只查官方文档、历史归档目录与覆盖元信息，确认一目标/一参考在拟议窗口是否同时有 1m 历史、最早可得时间/时间戳规则，以及是否有低成本 BBO/延迟证据；不读价格、不按收益选币，并由监督结合全局元信息裁定窗口资格。此项本次未执行。只有历史1m而无报价时，也可以提出明确标为“假设成本的初筛”的最多两次 native（一基线、一增量），无需先有实盘盈利或实测报价；但应先固定费用/滑点/延迟假设和判定规则。若收益仅来自 stale last-print 或零延迟边界假设，淘汰该可执行策略表述，不用改延迟或换币救结果。若廉价输入路径都不成立，以具名缺口结束，不先开发。完整 `PROPOSED` 仍须补齐固定目标、窗口、阈值、持仓、对照和成本参数后交监督审阅，本阶段不填假参数。

公开可得性口径：本次已核的是期刊正文与官方费用方法页可公开读取；作者报告曾取得其历史 Binance 1m 数据，这不是本次对现存历史下载服务的验证。指定 OKX 目标/参考合约的历史 1m 覆盖、同窗 BBO、实际 arrival/latency、历史合约/费率元信息及获取成本全部 `UNKNOWN`。现有 producer 源码是能力线索，不是数据可得或窗口未见证明。

无行情内存合成检查已通过：人工设定 60 秒调整滞后，5 分钟内冲击发生于第 60/120/180/270 秒，只有末个案例在第 300 秒仍有未完成调整；再加人工 60 秒延迟后四个均完成。另设 last trade=100 而可成交买卖价均已为101，则打印价显示1%“上涨”，实际可成交毛收益为0。**这些只是逻辑反例，不是市场观测、发生频率估计或经济反证。**

边界记录：未读任何行情文件、DB、ledger、旧消费索引或保护窗口内容；未运行 Generation、Candidate 审批或 native 回测；未修改仓库、配置、服务、凭据或 GitHub；未授予任何新窗口资格。N/K/L/O 与其他终态/封存信息仅取自本次委托，未重新核验。当前 session 的实际 model/reasoning/service tier 未回传；用户请求 GPT-6 Astra/high/Fast 与上游 config 已核为委托信息，不能当成本 session 实测档位。历史 memory 仅用于确认研究审计/不扩表约定，当前代码结论均重新静态核验。
