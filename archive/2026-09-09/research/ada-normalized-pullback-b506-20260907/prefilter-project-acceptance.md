# Issue 101 actual attachment acceptance evidence

PR 103 approved head `81e575f2db0bb60e43dd6b690bb7c63701461f98` was merged with `--match-head-commit`. Remote merge SHA is `fa6e49e4a0d9b53345821bf5312bf3443e7b68a1`. The worktree was detached at that merge commit before using the formal CLI. Untracked proposal documentation was preserved.

The CLI was invoked exactly once on this directory's authorized sanitized `lab.sqlite`, Candidate `fbf47a7c-c892-45c0-80ce-03bee437e1d9`. It exited 0. `prefilter-cli-import.json` records its result. Reviewed terminal SHA: `8f706f4271b40b325cb50ca15cbeacf951b32c1bacea86fa5576f9a2de26d9c6`; boundary SHA: `38aa824122333a413ffe99044c33494fa01a100536e8a32bbb404165b0377948`.

`prefilter-database-before.json` and `prefilter-database-after.json` retain the six-table counts and immutable-field hashes. Profile / Generation / Candidate / ResearchRun / Execution / Release remain 1 / 1 / 1 / 0 / 0 / 0. All other table contents, all original Candidate metadata subtrees, and all Candidate columns except metadata_json and updated_at are unchanged. Historical Generation COMPLETED and review APPROVED are preserved. The copied historical Profile display name was not corrected.

Existing Console PID 82694 on 127.0.0.1:8799 was stopped with SIGINT. `console-before-restart-command.txt` preserves its exact arguments. The same arguments and Python executable restarted the Console from the merge commit (exec session 61307). Startup performed existing source capability QC; this is metadata/data validation, not strategy evaluation or PnL computation. Startup warnings were deprecation warnings, not a failed check.

Actual HTTP GET `/api/generations/ef5de524-0506-4bd6-9af5-eec9d9c59202` and `/console` succeeded. `prefilter-generation-http.json` and `prefilter-console-http.html` retain responses. The API evidence equals the CLI result and stored database evidence. Browser CUA opened the actual `/console` read-only; its rendered accessibility tree's Generation normalized-state text displayed prefilter_evidence: PRE_SEARCH / UNDERPOWERED / S_SIGNAL_EXPOSED, total 14 / long 5 / short 9, required_total 24, UTC [2023-11-06, 2024-11-04), PnL null, native_search_runs 0, and the reviewed report/protocol/source/code hashes. No action button was clicked. The page's plain JSON detail is the existing renderer; no new UI or workflow was introduced. Browser content export was unavailable; the visible rendered tree is tool evidence, not a fabricated export or screenshot.

After restart the existing static Search capability refreshed from old startup BLOCKED_DATA to READY / SEARCH_READY because retained source now exists. `prefilter-search-context-http.json` records single-baseline one round / one attempt, empty attempts, null campaign_id and consumed_total 0. Generic maximum_attempts 6 remains a framework hard ceiling, not this batch's authorization. READY is engineering capability only; this Candidate remains UNDERPOWERED and no further Search is authorized. The actual page showed Development Candidate BLOCKED_SECURITY because no verified Search finalist exists; Holdout/Stress remain SEALED_UNREAD and FreqUI UNAVAILABLE. No native Search, Development, Holdout, Stress, Release or trading was run.

GitHub automatically closed Issue 101 at merge. It was immediately reopened on supervisory instruction with comment 5565685505. Supervision independently verified the immutable-field audit and actual API equality, then authorized final closure after the actual page check. CUA read-only DOM evaluation of the rendered pre element confirmed visible=true, Generation COMPLETED, review APPROVED, and the complete expected prefilter evidence. No UI action was submitted. Issue top-level text now corrects the earlier external-only closure claim. This acceptance does not authorize a new hypothesis or a replay; the overall qualified-strategy objective remains incomplete.
