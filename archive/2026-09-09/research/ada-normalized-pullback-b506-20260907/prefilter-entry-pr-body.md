Persist a reviewed pre-Search capacity failure on the existing Candidate without changing generation output, approval history, Profile snapshots, or creating a Search/ResearchRun. The existing Generation JSON detail displays the additional evidence through its safe text renderer.

Add one optional `prefilter_evidence` object, a strict attachment function in `codex_generation`, and a thin CLI. The total sample gate comes from the frozen Generation Profile; this importer deliberately handles total-capacity failures only. Reviewed terminal and boundary SHA arguments anchor the retained protocol, source provenance/receipt and report chain. Reads validate stored identity/shape; first write is transactional, identical repeats are no-ops, and differing evidence conflicts. File reads reuse the descriptor-relative no-symlink bounded reader. No new schema, state machine, network or strategy execution.

Scoring UTC endpoints are extracted from the frozen Search contract and cross-checked against source acquisition metadata and contiguous capacity-report blocks. Stored evidence validates UTC and positive duration.

Validation: 31 passed, 21 deselected, 0 skipped in 1.22s, including a real CLI subprocess on a temporary synthetic DB and real loopback HTTP GETs to the Generation API, Console page and Console script. Coverage includes approved-binding regression, hash/identity/window failures, immutable history, idempotence/conflict, injected write-abort rollback and file/type/read-tamper rejection. This is HTTP verification, not browser visual verification. No real research DB write yet; after review/merge, Issue #101 still needs the authorized CLI attachment and actual API/page verification. Do not close #101 with this PR alone.
