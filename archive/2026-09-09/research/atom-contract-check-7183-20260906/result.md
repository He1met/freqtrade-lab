# ATOM 单合约历史契约核验：NO_GO

2026-09-06 03:50–03:56 UTC；仅元数据与本地静态源码检查。**该有限历史契约未成立，停止此路线的工程/采集推进；这不是策略经济失败。** 四次实际公共请求全部 HTTP 200，零自动重试，预算 4/16；未下载 ZIP，未获取 OHLCV、mark、资金费率值，未读取封存数据、入库策略或调用 native。原仓库无修改。

**选择与占用。** 网络前按既有 XLM→ATOM 值前顺序锁定 `ATOM-USDT-SWAP`，未循环换币。此前有限身份投影未命中 ATOM；这支持“未发现已知冲突”，不能证明全部合法历史已确立。XLM 的旧 S 属于实际消费，D 是已物化/QC但未经济运行，H 是预留且未取得；后二者不能改称已消费，也不在本轮释放。BTC/ETH 2020–2025 已知训练暴露与 2025 外层封存分别保留，不推广到 ATOM 全部历史。全资产 `[2026-05-31,2026-07-31)` 仅沿用监督指定的 UNKNOWN 排除边界。未全文重审账本。

**日历容量（UTC，左闭右开）。** 下列仅拟核方案，不是已获数据或冻结质量门：

| 阶段 | 精确评分区间 | 天数 | 365 日前史起点 | 月度日历机会 / 本阶段月末信号可在阶段内下一开盘执行 |
|---|---|---:|---|---:|
| S | 2021-01-01 00:00 至 2024-01-01 00:00 | 1095 | 2020-01-02 00:00 | 36 / 35 |
| D | 2024-01-01 00:00 至 2025-01-01 00:00 | 366 | 2023-01-01 00:00 | 12 / 11 |
| H | 2025-01-01 00:00 至 2026-05-31 00:00 | 515 | 2024-01-02 00:00 | 17 / 16 |

两列次数差在阶段首日是否允许使用阶段前信号；本轮未授权该执行语义。12 个月动量可能长时间同方向，重发信号不是新交易，每月强行平开也不能创造独立样本。**一年 D 不足以事前承诺足够趋势反转/独立验证**，本轮不沿用 XLM 的六自然交易、90 日组数门。即使未来数据齐全，评估也应保留月度净盯市与相关性/有效样本量，而不能把 12 个数当作 12 个独立样本。当前没有证据支持仅通过重分配这段日历就获得合格容量。

**官方已证实与边界。** [instruments 元数据](https://www.okx.com/api/v5/public/instruments?instType=SWAP&instId=ATOM-USDT-SWAP)返回线性 USDT 合约、live、`listTime=1573557408000`（2019-11-12 11:16:48 UTC），早于前史。`contTdSwTime=1611916860000` 非空，但[官方字段定义](https://app.okx.com/docs-v5/en/#public-data-rest-api-get-instruments)称该字段适用于 SPOT/MARGIN 从集合竞价/预报价转连续交易。SWAP 返回此值的原因 **UNKNOWN**，不能当成 2021 年首次上市，也不能据 listTime 证明实际连续历史。

对锁定合约先查 2020-01 至 2020-06 前史目录，随后单独核查真正评分所需的 **2021-01 至 2021-06**；两个响应均 `code=0, details=[]`，无下载链接。这只证明本次官方目录接口未提供链接，不能推断整个交易所从未存在这些数据。第二次查询避免把“不需要计算持仓费的预热期缺档”误当评分期失败。已有证据不能建立完整 S 资金费源，所以停止后续月份/公告请求；未通过不断向后移动起点挑目录。

API 文档确认 `fundingTime` 为结算时间、`realizedRate` 为实际费率，并允许频率变化；**ATOM 在该完整历史区间的确切频率、时刻、变更边界及 archive 时间戳偏移含义均 UNKNOWN**。2 秒以内偏移不因此有效。若日后取得其他正式档案，仍需值前确认事件日历、实际费率字段、mark 覆盖和精确 join、开平同刻归属；缺费不得填零。当前不提出归整新规则或修改旧失败回执。

**静态入口与资源。** 仅对内存源码运行 `analyze_bounded_causal_strategy`：`shift(365)` 在 startup=365 时拒绝，在 startup=366 时通过；UTC `.dt.is_month_end` 和 `.dt.day` 被 `EXPRESSION_OUTSIDE_TEMPLATE` 拒绝。未执行/注册策略。按当前静态契约，启动前史还应各加一天（S 2020-01-01、D 2022-12-31、H 2024-01-01），对应日线 1461/732/881 行；合并历史包 2342 个日历日。保守全前史 mark 上界 56,208 小时行，**仅在全期固定 8h 假设下**为 7,026 个资金费事件；这不是实际事件数。全月份目录最多 13 组六个月请求，但已在首个评分期组缺链接后停止；真实采集请求预算尚不具备冻结条件。

若数据证据另行解决，工程缺口才值得处理：`lab/bounded_strategy.py` 有限 UTC 月末表达式；`lab/bounded_research.py` 多年 futures 1d 资源边界；`lab/holdout_run.py` 与现有 native wrapper 的 futures 1d H/Stress 同 run 入口；producer 的**该 instrument/该区间**有限事件契约。后者是核算缺口，不能由前三项工程通过替代。本轮均未改；不建通用变频系统，不改 native。

**下一决策：停止为本方案追加同类审计或合约工程。** 若仍研究此同一假设，需要先取得官方可追溯的缺失历史档案及时间戳/频率说明，这是新证据，不能用现有空目录继续推进。若该历史无可得来源，应改变数据范围或改用能在较短、可得历史内形成更多独立事件的经济机制，重新论证成本与相关性；不等待未来、不机械换币，也不把 12 个月动量改几个参数冒充独立新机制。本轮没有选择或运行替代方案。

证据文件：[请求账](requests.jsonl)、[生命周期](instrument-metadata.json)、[静态与容量](local-capacity-static.json)、[2021 目录](catalog-202101-202106.json)、[官方字段摘录](official-doc-excerpts.json)。目录响应 SHA `fcca4382ef6212fa2acaaeb17e1681e2a0b4f57a8832b9e1d8683de99ad5ab44`；身份响应 SHA `9a548acc4b76496d02ecf83ab6ea8d80b53deae1d978f48dc8ec1304784341a0`。
