# Stage A data decision

**Supervisor accepted bounded engineering GO and continuation to Stage B. The original proposed full S scoring window is not eligible under an exact-event fee uncertainty contract; the smaller source-covered proposal is accepted for later research freeze.** No strategy was run and this is not an economic verdict.

The unchanged locked native downloader, bounded at transport level before each request, obtained all 378 daily candles, 9072 hourly mark candles and 1134 funding events for [2023-07-03,2024-07-15). Identity, actual response timestamps, unique observed 8h buckets and raw/native funding join counts passed. The official August archive and CHECKSUM match all 93 REST event times/rates. Source evidence lives in native-*-http-receipts.jsonl and their retained decoded bodies; these are not wire bytes. Full-s-qc.json records exact coverage and approximation checks.

Official developer documentation retrieved through the web tool's normal redirect identifies the history endpoint, millisecond inclusive query limits, ascending pagination and fee-associated mark. Direct local HTML/schema requests returned 202 empty and are retained as failed captures, not semantic evidence. Reference: https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data . Official llms.txt was locally captured with SHA in the work outputs. The monthly archive includes funding_interval_hours=8 and identical REST event times/rates; this supports the sampled settled-history mapping rather than treating a forecast API as final funding.

Native normalization changes some 0–15 ms timestamps to the minute. All 1134 events survive normalization and join, but inclusive open/close accounting has membership ambiguity at event boundaries. Synthetic checks verify both endpoints and long/short fee signs. Historical partial-candle handling removes the last full bar even for a closed range; source packaging must retain native original files and separately restore only the already-fetched, verified complete raw bar through the native converter. No missing-bar fill is allowed.

361 early fee-associated marks are missing; the last missing event is 2023-10-31 00:00:00.001 UTC. Known associated marks demonstrate that two marks lie outside event-hour high/low, so the hourly envelope hypothesis is falsified as a strict bound. Exact known mark/native fee discrepancy remains directly measurable. Therefore use a versioned contract requiring associated marks for every scoring funding event; preserve raw timestamps, use conservative additional deductions for event price difference and uncertain entry/exit membership in the actual qualification path. Missing event/mark or source mismatch fails before research mutation. No native matching engine reconstruction.

The smallest proposed eligible S begins Monday 2023-11-06 and ends 2024-07-15 (252 days, 36 weekly slots) with existing price prehistory. It is a source-coverage change made before any economic result, not a performance-driven date choice. Supervisor freezes research dates/capacity later; engineering can implement this source contract now. D/H/Stress still require separately acquired valid sources and the same run; no qualification or release is implied.

Remaining Stage B: integrate existing CHECK/Profile/source/native offline runner/artifact/qualification/continuation/console with tests and actual entrypoint acceptance. No business code changed yet. Issue stays open. This is not completion of the overall plan.
