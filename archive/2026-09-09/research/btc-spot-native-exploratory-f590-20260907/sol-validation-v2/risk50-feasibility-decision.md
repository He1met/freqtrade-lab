# 固定低暴露版本：训练可行，完整验证须先授权保护合同变更

本次仅控制面决定；没有计算50 USDT档原曲线，没有新HTTP/native/信号，没有修改Profile、源码、原协议或台账。

**决定：可以用现成入口做一次明确标注的已见SOL训练；当前权限下不能立即执行完整独立验证。唯一建议提交的实质决定，是是否把本次已否决v2的未读取D/H重新预约给一个事前固定的新风险假设。** 不需要新系统、字段或Issue。旧Candidate `99eb3df4-ebad-4d97-8dfd-0fdd2af826ce` 永久保留本轮REJECTED，不因新研究而转正。

## 1. 训练路径现成，但不是独立证据

新假设只把固定名义仓位改为50 USDT、初始钱包1000；这是初始钱包的5%，**不是每根K线按实时权益动态调仓5%**。信号、28日动量、29根预热、8%止损、每边.0012费用代理及源码SHA `e1ab5c109eded7a6bd0e4e5d7b07f6c8e3082640530129ea9a390a2f89944611` 保持。50来源是简单账户预算，不从已见10%门反解最优参数；尚不知道它的实际收益或回撤。

现 `fetch_okx_profile_data.configure_profile_acquisition` 可读取新的exploratory window，令Development=None；`bounded_research.validate_profile_search_contract` 明确允许探索无D，禁止与SINGLE_BASELINE混用。`validate_profile_runtime_contract` 接受正数且不超可交易钱包的固定stake。`complete_generation`按当前DB的源码SHA去重，因此应使用新的隔离研究DB/Profile及普通探索Generation，不在旧库换标签、改旧审批或复用旧失败Candidate。

如后续授权，训练计分仍为 `[2021-03-01,2022-02-01)`；fresh原始源只采 `[2021-01-31,2022-02-01)`，366根日线。新窗口文件须使用exploratory schema，S start=2021-03-01、development_start=end=2022-02-01，显式绑定exposure审计：原SOL S完整结果及100/150/250缩放诊断已见，基于这些观察作风险校准，外部暴露UNKNOWN，NOT_INDEPENDENTLY_VALIDATED。**fresh acquisition改善来源绑定，不会恢复数据独立性。**

准确拟议采集预算：1次调用、最多5 GET（1次SPOT instruments＋4页日线，每页≤100）、0重试、900秒、decoded和目录各50MiB响应后/最终检查；失败停止，不补抓。新的监控wrapper需绑定该窄窗口。后续探索Generation1、主native1，禁市场smoke/R2/参数网格；探索父选择或READY不冒充finalist。当前这些调用均未授权。

## 2. 不动保护合同，没有当前完整验证路径

已核全局ledger SHA `b04a689820be336826c255ee9835b0b1db4c2fcd31bbf8f8a897c5aed9b5f90c`。在此次限定SOL路径内：2021-03→2022-02已经研究读取；本次v2 D `[2022-02-01,2023-02-01)` 被旧协议保护且仅机器采集/QC；H `[2023-02-01,2024-02-01)`受保护且未采集。更后SOL旧channel源从2024-02-01开始，S与旧D/H覆盖到2026-08-31，保持原约束。上市前不可用；上市后到本次S之前不足形成完整三阶段；2026-08-31之后当前短空档也不足，并会使29日预热跨入旧保护段。没有据此扩展到别币或搜索更多机制。

这不等于D/H价格已污染：D机器QC没有产生研究者信号/经济结果，H未采集。先前看到同资产S以及BTC/ETH同期学习仍须披露，最多主张资产级条件验证；再次训练增加自适应选择偏差，不能声称全局未见或独立alpha。

若批准重预约，给一个固定、完整的三窗提案，不另选日期：

| 新阶段 | 计分半开区间 | 29日预热起点 | 日数 / 日历周一 |
|---|---|---|---|
| 新S | [2022-02-01,2022-08-01) | 2022-01-03 | 181 / 25 |
| 新D | [2022-08-01,2023-02-01) | 2022-07-03 | 184 / 27 |
| 新H/Stress | [2023-02-01,2024-02-01) | 2023-01-03 | 365 / 52 |

新S/D由旧v2 D按日历固定两段分开，新H沿用旧v2 H的时间范围；**这是对原保护合同的变更，尚未获准，不是发现了现成空闲窗。** 新D起点周一不能直接入场，其27个日历周一也非27个可执行机会。每段至少6笔/三段至少18笔仍不降门，短S/D可能UNDERPOWERED；不延窗补样本。新S预热复用已见训练尾段，仅作历史指标，不计分；各阶段重置钱包。旧channel自2024-02-01后的保护窗完全不涉及本提案。

新正常验证还需另一个独立新DB/Profile/Generation，不能把探索Candidate换成正常模式。新source/newSINGLE_BASELINE协议事前冻结，保持成本后净利>0、PF≥1.10、钱包open-DD≤10%、去最大赢家后净>0和已接受的同成本风险比基准；基准名义仓位必须与新的主仓位同为50，其它计算约定不变。极小仓位只会压缩亏损，不能替代这些正收益/费用门。现入口要求正常S和D同时冻结，H由同一合法D ResearchRun终点＋holdout_days=365派生；H+Stress一次续跑的限制不变，后续仍逐门另批。

如后续另批采集，新正常S+D源为 `[2022-01-03,2023-02-01)`，394根、1次/最多5 GET；H-only源 `[2023-01-03,2024-02-01)`，394根、另1次/最多5 GET且只能完整D通过后。各自0重试/900秒/50MiB限额，全部采用fresh receipt，不改旧raw provenance。该数字只列可执行预算，当前许可仍为0。

## 唯一需要决定的问题

**是否明确允许将旧v2协议 `cfebbdc938ac494426ec7e0fcc08fc59d09f45c6335c631d42a53db8409e2d1c` 的未读取D/H，按上表一次性重新预约给“固定50/1000风险版本”的新研究，同时保留旧REJECTED，并禁止旧研究再使用这些保护窗？**

若同意，应先追加原保护预约终止/新预约的授权证据（保留旧字节与SHA），再创建新冻结包；不在旧协议或数据库上回填。现采集和研究入口只验证新Profile/来源/阶段绑定，不会替用户批准跨cohort窗口再分配，全局ledger与root明确授权是必要控制。该实质决定也不自动授权HTTP或native，训练与新验证的有限预算仍须分别明确。

若不同意，则当前路线结论为：已见训练技术可行，但没有已获准的完整即时验证路径，应停在这里，不消耗训练后继续机械找窗。即使同意，任何固定50训练的数值QC/成本或风险门失败都结束本假设，不再试更小仓位救结果。
