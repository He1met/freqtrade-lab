The Profile producer accepted the frozen 610-day spot daily Search and 547-day Development windows, but consumers rejected them at a fixed 366-day limit. This change consistently allows validated spot 1d Profiles up to 1830 days per phase, with at most 512 warmup candles (2342 daily rows). Legacy 5m and futures 1d remain capped at 366 days, and the existing fixed 60-day Development / 30-day Holdout branches remain intact. These are resource limits; strategy windows and economic gates are unchanged.

Three lab modules share the narrow check: Profile contract/plan/data validation, all three Development window consumers, and Holdout source authorization before file writes or market reads. No producer, native runtime, schema or strategy change.

Validation: 227 affected tests passed, including 15 new boundary cases; no native backtest was run. Tests exercise 547-day D freeze/snapshot/materialization and the actual H authorization path rejecting excessive duration/warmup without DB or source side effects. The existing real source then passed `prepare-search-data`, producing 700 Search-only daily rows (610 + 90 warmup); all eight original source files retained their hashes. Actual Console GET reports SEARCH_READY, the sole frozen Candidate, zero consumed attempts and SINGLE_BASELINE_V1 active limit one. Search has not been started; no economic result or new ResearchRun exists.

The original consumer exit-2 receipt is retained separately from the successful QC. Runtime evidence is outside Git at `/Users/shenjianpeng/.codex/runs/freqtrade-lab/issue-90-xlm-sma90-v1/`, including source identity before/after, QC, plan preview and actual HTTP context. The plan preview does not consume the campaign; the existing POST will assign the actual campaign ID only after separate Search authorization.

Refs #90. This PR covers only its necessary consumer resource fix; the research Issue remains open. Awaiting supervising review; no merge or Search authorization requested implicitly.
