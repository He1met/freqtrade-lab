# BTC/ETH 永续独立确认窗口审计 V1

本次是用户追加“执行所有优化”后的元数据审计与事前冻结。没有读取新的市场价格、重跑回测、采集未来数据或启动确认。当前优化回合属于 **USER_STEERED**，不能充当首次自动 heartbeat 的成功证据。

## 窗口结论

没有找到可直接证明合法且未见的历史共同窗口，因此采用真正未来：**2026-09-10 00:00 UTC 至 2026-12-09 00:00 UTC，连续90日**。这不是从旧 #139/#155 日历继承授权；现货观察已经归档，新永续协议单独登记。BTC/ETH 同期只有一个候选、一个共享研究钱包与一个计算 worker，确认结果不用于重新选择候选。

| 已核证据 | 具名区间 | 本次裁定 |
|---|---|---|
| 中央知识库 `btc-eth-1000-central-v1.json` | 2023-11-01 至 2024-11-01 | 明确已曝光，仅开发；旧研究 `UNDERPOWERED` 不改 |
| 同一知识库 `windows.sealed_*` | 2024-11-01 至 2025-01-01 | 旧封存，未解除、未读取；不是本次可直接领用的 Holdout |
| round-001 原生 manifest、首批协议和当前知识库 | 2025-01-01 至 2026-07-01 | 已完整用于本轮开发，包括2026年后段；不可更名为 Holdout |
| 旧 #119 来源限制快照 | 2025年和2026-05-31至07-31含历史 GLOBAL 标记；BTC另有2026-08-01至09-01具名限制 | 快照是旧候选范围投影，不能充当最新全局登记；保留原约束，不以换交易所/产品宣称独立 |
| 其余过去窗口 | 未建立完整未见/可用证明 | `UNKNOWN`，本协议不准入；这不是断言所有历史永远不可研究 |

快照还列有只针对 SOL、LINK、XRP 的限制，不能泛化成 BTC/ETH 的永久禁令。旧限制记录和封存市场值均未修改。本次只读取本地索引、已脱敏协议/终态的窗口元数据，不重建整套历史审计，也不访问敏感数据库或凭据。

来源指纹：中央库 `7469c8970be61d87aa43d6a15507a12434ba0e56768d596c8ea10115cdeda69f`；旧 #119 快照 `03c14224fefb254285d22331f9109fcf8fa9033dfba85b5c888032be3fa6ab7c`；#131终态 `4421e64a235668f2b032995e243bdc638057c7b026a59bb05c066b4cc1e2fa66`。这些指纹说明审计读到了什么，不替代最新登记或经济许可。

## 事前承诺与当前阻塞

[机器可读协议](../protocols/perp-independent-confirmation-v1.json) 在 V2 市场结果前写入。候选范围仅为父版 persistence 12h 退出及已计划的24h/48h退出 V2；完整开发净收益正、原生语义与成本核算可复核才进入候选排序。按开发净收益选择最多一个，1USDT内近似并列优先低换手、再优先短退出窗。没有合格候选就明确 `NO_COST_SUPPORTED_CANDIDATE`，不临时增补机制凑确认。

冻结时刻 `2026-09-08T15:52:02.900919Z`，协议 SHA-256 为 `60ca5096404bb0f33a9fdbf36b74336a8f4cf62ee5107811613feb6ece4710e5`。写入后校验确认90日主窗、7日暖启动、末72小时禁止新入场及未激活状态；当时只列实验目录名，除 `round-001` 外没有 V2 实验目录，未读取 V2 市场结果。

选中代码、协议、成本、来源、开发报告及其 SHA 必须在 **9月10日00:00UTC之前**登记。否则本窗口 `NOT_ACTIVATED_MISSED_START`，不得补登记或静默后移。7日暖启动为9月3日至10日，仅计算特征；尚未发生的暖启动尾部没有被取得，也没有被称作已曝光结果。初始空仓，末72小时不再开新仓，按固定规则到期结束，不按近期收益续期。

当前状态为 **FROZEN_WAITING_CANDIDATE_AND_TIMELY_SIGNAL_RUNNER**。仅有30分钟 heartbeat 与小时采集文件，尚不能证明及时决策。激活前必须真实跑通增量数据、可用时间、预先持久化 signal/NO_TRADE、原生对账与单writer恢复。信号至少在计划入场前写入；迟到后用完整行情重放只得 `FUTURE_DATA_DELAYED_REPLAY_ONLY`，不能伪装成本协议的及时确认。原生离线成交假设下的未来结果仍不证明实际成交或实盘保护。

约20%是风险目标。20.1%或21%不机械淘汰；风险作为独立维度报告。本次预先设30%为必须重新定标和复核的保守研究触发点，不能解释为用户已接受30%实际账户风险，也不能据此改生产保护。所有风险定标必须事前改版并重算完整钱包。

90日按同步BTC/ETH的72小时块记录，共30个日历块。至少12个非空块是有限确认的计划下限，不是行业标准或显著性保证；自然机会不足即 `UNDERPOWERED`，不制造交易、不要求每天开仓。完整数据、及时决策覆盖、净收益方向和风险分开判断：正毛效应成本后负为 `COST_NOT_SUPPORTED`；足量且完整成本后正只得 `CONFIRMATION_POSITIVE_LIMITED`；压力负另标 `COST_SENSITIVE`，不承诺可实盘。置信区间如能计算按协议固定块长/种子处理，缺数据就 UNKNOWN。

## 自动触发证据

新自动化配置 ACTIVE 与实际运行来源是两件事。此次优化由用户明确追加要求触发，保存为 `USER_STEERED_EXPLICIT_OPTIMIZATION`。真实首次自动触发与真实下次触发均为 **UNKNOWN**。

后续收据须保留 automation ID、目标任务 ID、实际 turn ID、started_at、调度触发来源字段及其证据路径、原因分类与状态检查点 SHA。只有管理接口或提供方明确标识自动触发，才登记 heartbeat 已发生；不能根据提示词相同、恰好相隔30分钟、手动 tick 成功或启用状态反推。现有管理 `view` 只向模型返回已渲染卡片，未提供该字段；不绕过工具限制读取内部数据库或猜测。
