# 发现阶段计量与强平资料待核清单

用户经监督任务授权完善计时口径，并继续有界核验强平机制。此次只增加一个本地追加事件入口及分项协议；没有新计时服务、平台、行情采集路线或原生回测；沿用必要小时维护。新入口不修改预算或scheduler。

## 保持的计费口径

| 阶段 | 记录的工作 | 原口径处理 |
| --- | --- | --- |
| SEARCH_READ | 查询构思、调用及其等待/失败、阅读摘录、机制判断 | 计入发现处理时间 |
| REPORT | 依据现有事实整理卡片、摘要和紧凑终态 | 单列，但仍计入发现处理时间 |
| MAINTENANCE_WAIT | 事前暂停发现后进行必要维护或等待维护门 | 默认保守计费；完整证据核清后才可依原政策排除 |
| USER_IDLE | 实际停止发现并明确等待用户 | 默认保守计费；普通思考或工具等待不属于此项 |

UTC与monotonic纳秒、boot身份由CLI实际记录，无回填时间参数。两种经过时间分别保留；同一boot且差异不超过2秒时按两者较大值保守计量，超差保持未知。`phase_wall_seconds`为UTC观测，`phase_conservative_seconds`为计费依据。阶段切换同时结束旧区间并开始新区间，不凭事件之间的空档推断空闲。单调时间测的是经过时间，不是CPU或模型费用。

入口仅显示分项与保守总量，不自动扣减或释放预算。等待证据文件的SHA匹配不足以证明可免扣；还须核实际起止覆盖与无发现重叠。开区间、时钟不一致、重启或引用变化需要对账，保留未知与预约，不能拿已知小计冒充完整耗时。

旧扣费全部保留：1059.921780秒历史墙钟代理＋91.806242秒首批连续发现＋33.149510秒自动来源检查＝1184.877532秒。旧记录混合读取、解释、报告等工作，无法可靠追溯拆分，精确主动/CPU时间仍NULL；本次未退款。每日上限仍12查询、16页面、1200秒，当前9查询、13页面、剩15.122468秒。

## 后续实际入口

使用固定native Python及`PYTHONDONTWRITEBYTECODE=1`运行`scripts/perp_discovery_timing.py`：

```text
begin  --log <批次.timing.jsonl> --reference <不可变.started.json> --purpose <批次说明> --phase SEARCH_READ
phase  --log <同一日志> --phase REPORT
phase  --log <同一日志> --phase MAINTENANCE_WAIT
phase  --log <同一日志> --phase SEARCH_READ --evidence <维护回执>
finish --log <同一日志>
status --log <同一日志>
```

在查询构思前随独占starter开始，阶段转换先于工作。REPORT的紧凑终态内容准备完后才结束；后续交付保留计时日志SHA。所有路径在专用Git外运行根，start不能复用旧文件，finish后不能继续追加。原搜索/页面事件、日累计和未知预约继续单独保留。

本次计量入口验证属于另行授权的工程验收，绑定`discovery-timing-v1/authorization.json`，不冒充一次新资料核验，也不把未完成发现报告改类来省时。后续真实发现批次能否按阶段完整留存，须等待其实际执行记录；不以工程验证代替自动发现验收。

## 强平问题：已有证据与未决部分

此次只复用已读证据，并检查开发准入manifest元数据，没有再次请求旧失败URL。

- **原始机制依据未建立**：尚未实际读取能支持范围内强平后至少1h可证伪价格效应的原始研究。
- **当前事件数据未准入**：manifest `06db3dc5…f4c`只包含BTC/ETH OHLCV、mark、资金费及来源/合约规则，不含强平事件、事件完整性或历史可用时点。因此市场执行分支为`BLOCKED_DATA`，不得用普通OHLCV冲击替代。
- **来源验证仍受阻**：上一批官方路径只返回通用门户；字段、历史覆盖、准确时间戳、当时可用性及免费访问条件均UNKNOWN。尚不能据此断言不存在公开历史数据。

唯一下一资料动作是定位有依据的替代官方路径/官方仓库副本，再核原始机制与数据条件。建议一批最多2查询、3页面、240秒；这些是预约上限，不保证能完成全部核验。今天如立即执行，需要额外224.877532秒时间额度，查询/页面上限不必提高；**本次未授权、未申请使用或自行增加该额度**，默认上海2026-09-10 00:00日界后复核。

只有机制与合法事件数据同时成立才事前冻结有限对照。完整有界核验后仍不满足数据门，就明确`BLOCKED_DATA`并停止此数据依赖分支，写清解除条件，再选择一个不同、未查的问题；没有可辩护后继允许空槽。工具未返回或处理未完成时不能声称核验完成。固定carry_nonpaying的候选、90日窗口、经济规则和单writer保持。

## 实际验收与调度读回

6项定向unittest通过（0.115秒）：覆盖阶段连续、报告及等待默认计费、未知开区间、时钟回拨/重启/身份缺失、容差内UTC偏短、重复开始/终态追加、锁忙、绑定漂移和真实CLI。复核中发现并修正容差内只累计UTC可能低计的问题；例如UTC 9秒、monotonic 10秒现在按10秒计量。没有运行旧市场对照或全仓库回归。

05:10:58.335320Z至05:11:03.295639Z实际完成begin→维护阶段→REPORT→finish/status。UTC总经过4.960319秒，其中维护4.913866秒、验收结果整理0.046453秒；SEARCH_READ和USER_IDLE均0。最终保守总量4.960319041秒，默认排除0秒，COMPLETE且无未决预约。此短区间仅验证工程入口，并非本轮全部工程耗时或一次真实发现批次；CPU和模型费用NULL。原日志与旧汇总保留，新代码只读复核结果另存`meter-status-conservative-v1.json`，未重跑维护。沙箱无法读取boot时的未知分支也已覆盖；获准运行环境的本次真实boot证据完整。

该维护实际15次公共市场GET，观察记录累计14个双币小时，最新提前意图健康TIMELY_COMPLETE，仍PRE_CONFIRMATION；新增外部资料查询0、页面0、native 0、订单0。这些是维护/工程证据，不是强平资料核验或经济验证。

执行自动化`freqtrade-lab-2`已通过管理工具更新并逐字读回：ACTIVE、30分钟、原执行任务保持；提示SHA `b0be0f311941ccdc2545810586bc55b068f71bf66b3d1d90a8d6022f3d5037a1`。`btc-eth`监督自动化文件未变，未新建调度器。实际下一provider触发时刻不可见，保留NULL，不从间隔推算。回执为`discovery-timing-v1/automation-readback.json`，SHA `4a89562fa250269883220c5b3a0140bb20f58578da12e5cfdb4c890b51681f94`。新增计时提示尚待后续真正发现批次验证，不声称已自动完成阶段计量。

05:16:52Z实际只读状态入口确认writer IDLE、探索WAIT_DISCOVERY_BUDGET、固定确认WAIT_FORWARD、观察TIMELY_COMPLETE，未暴露确认经济值。验收包装首次把确认的reason误作status导致断言失败；核实际字段后修正包装并通过，产品代码未因此变更。预算收据仍原SHA，固定候选、政策与19项确认代码绑定不变。
