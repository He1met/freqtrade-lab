# 前向原生消费者：工程完成，真实验收等待输入

已完成最小消费切片，复用原生 Freqtrade 撮合、共享钱包和精确资金费；新增部分只核对提前持久化的意图和屏蔽缺失时段。固定候选 `carry_nonpaying` 的参数、成本和未来确认协议保持原字节，未建立交易引擎或账户接口。

定向测试只发生 **1 次合成 native 调用**：首笔双币订单严格为预定 17:00 UTC，前置三小时空仓只用于补齐 168 个收益观察，缺失的 19:00 意图不补录；20:00:00.005 的合成资金费事件、实际原生费用和钱包逐项对账通过，终末成交时间为原生 23:00 `force_exit`。pytest 外层 1 passed（2.80 秒）；之后只运行 2 个纯输入/信号测试（1.477 秒），证明缺资金费查询覆盖会阻断、迟到记录不计及时、自然信号冲突及源 SHA 漂移会拒绝。没有第二次合成 native，也没有真实市场 native。

真实默认 CLI 已核验，返回 **`WAITING_DATA`、`data_sha256=null`、`native_calls=0`**，预检耗时 1.273347 秒。执行代码聚合 SHA 为 `fddff29e9e5165748f0f5169f5968d70e1a73e15cb3f8541e474f63375af30c1`，当前执行 policy V3 SHA 为 `7610af9261f8ff6e4adc12f6045e8d81597b8d11dd2954cf922b026cbdf43646`。实际预检原件 `/private/tmp/perp-migration-v1/acceptance-preflight-v1.json` 的 SHA 为 `3328c636c58944387bc588e6c386dd59769e89bedf4a6be66ba45326bb464cce`。真实七小时窗尚未完成，最早 2026-09-09 00:10 UTC 才可能冻结实际输入，不能把准备契约当作已经取得的数据。

已只读核验首个真实 17:00 小时观察收据：16:40:26.706541 UTC 持久化，两币都是完整输入的 `TIMELY_NO_TRADE`，阶段 `PRE_CONFIRMATION`，native=0、orders=0。这说明真实意图留存已发生；它没有执行原生验收、激活正式确认或证明自动调度触发。

两个准备阶段错误保留：首次合成 fixture 的 `/var` 与 `/private` 路径别名导致输入检查失败，修正为规范绝对路径后通过；一次可选系统 Python 编译检查因外部缓存权限失败，随后用固定 Python 3.13 的只读 AST 解析通过。两次错误均发生在 native 前，未触发市场重试、数据重抓或依赖更换。原生独立计时没有单独采集，保留 null，不用测试总耗时冒充。

唯一下一步是按固定窗口完成真实消费者验收，流程见 `docs/research/perp-forward-acceptance-runbook-v1.md`：实际输入冻结 → materialize → 唯一 claim → 一次 native → 中文报告及 finish → 单独校验激活 → 更新唯一后继队列。缺失或迟到意图保留，零成交只能得到受限工程结论。正式 90 日期末确认另有冻结窗口，当前工程通过不产生经济资格；21.45% 回撤、水下时长、ETH 空头集中和成本压力余量仍需未来验证。

机器证据见同目录 `perp-forward-native-engineering-v1.summary.json`。本交付的真实市场验收次数为 0，盈利验证结果及实际货币成本为 null；调度真实状态由根任务的持久队列收据单列。
