# carry_nonpaying：已冻结未来确认准备，尚未确认或激活

第三轮 `carry_nonpaying` 的真实原生开发结果为净收益 **+80.18 USDT（+8.02%）**、毛价格损益 +222.66 USDT、资金费 +6.75 USDT、249 个持仓周期和 138 个共同 72h 入场簇。逐事件费用和统一钱包对账通过，足以把它选为**一个待未来确认的研究候选**，不构成盈利资格、影子准入或实盘权限。

风险并不宽裕：小时 MTM 回撤 **21.45%**，最长水下 **10,748 小时**；每侧再加 4bp 的同成交压力后只剩 **+5.56 USDT**。原生已实现净贡献按币种/方向交叉分解为 BTC 多 +15.24、BTC 空 −25.46、ETH 多 −118.85、ETH 空 +209.25 USDT，盈利集中在 ETH 空头。保留 BTC/ETH 与原有双向规则，不因这些结果删掉亏损方向或放大杠杆。

约 20% 仍是研究目标，21.45% 归类 `ABOVE_TARGET_REVIEW`，没有机械否决；原协议 **30% 必须重新定标复核**的规则保持不变，实际交易风险未放宽。长时间水下、方向集中、成本压力余量很薄和小时内风险未知，都必须在未来报告继续披露。

新协议 `docs/protocols/perp-independent-confirmation-v2.json` 于 **2026-09-08 16:10:13.068639 UTC** 冻结，SHA-256 为 `e98c56f68f4a7f3390b4fdbb06425500a4c977edeae4b20489c2c4d5d4319dc6`。候选注册文件为 `docs/research/perp-carry-nonpaying-registration-v1.json`，SHA 为 `a4ca6146c9fed5e56d2370d22f8c8c4dd5ea8ac478a0f1c6429bbe3bff087460`，绑定第三轮实际策略、runner、成本、政策、数据与真实原生结果 SHA。

未来窗口仍为 **2026-09-10 00:00 至 2026-12-09 00:00 UTC，90 天**。原 V1 从未激活、没有候选或确认观察，窗口尚未开始，因此 V2 明确替代其未激活预约。旧 V1 文件及 `60ca5096...` 原 SHA 保留，没有改写旧证据或重新利用评分数据。原 90 日边界、95% 及时覆盖、12 个非空同步 72h 簇、8bp/侧基础成本、风险分类和终态判据逐字段继承验证通过。辅助提前一天取得 8 日预热仅用于工程就绪，不增加评分期或计算预热收益。

新版本只准入 `carry_nonpaying` 家族。它保留 24h 突破、方向持续性 0.25、12h 反向退出、最长持仓 72h、1,000 USDT 研究钱包、1x 与原成本。新增的主要因子是当时可知的已结算资金费符号：多头要求最新可用费率 ≤0，空头 ≥0。同小时 premium 仍是原实现的完整性条件，其**符号不参加选择**；不得把 OI、链上或 premium 符号临时加进主确认。

时序区分名义选择时点与真实观察时点：源 candle t 的 `nominal_cutoff=t+1h+60s` 只选择原模型当时已发布的记录，预定执行仍为 `t+2h`。真实决策采用实际观察与必要输入抓取完成时间；heartbeat 在 +10/+30 分钟取得资料，可以在预定执行前如实留存，但不能倒签成 +60 秒。资金费年龄仍按名义 cutoff 与实际结算时刻之差 ≤12h。晚于预定执行的记录归为 `LATE`，不补录及时信号。

及时覆盖分母固定为 **90×24×2=4,320 个 pair-hour 槽**，包括空仓、自然不交易及最后 72h 禁止新入场的时段；至少 **4,104 个**必须有完整输入且提前持久化的及时 SIGNAL/NO_TRADE。缺数据即使及时记录，也不冒充有效 NO_TRADE，不进入分子。两币同一 72h 块和成本情景不当作独立样本。

当前状态为 **`WAITING_NATIVE_ACCEPTANCE`**。今天可留存真实 `PRE_CONFIRMATION` 工程意图，但不能激活正式确认。用户后续授权的 V3 政策将 `perp-forward-native-acceptance-v1` 单列为固定候选工程验收，不占探索变体。保留事前选定的 2026-09-08 17:00 至 2026-09-09 00:00 UTC 七小时真实意图窗，收盘后真实数据齐备才可领取并执行；此等待来自输入尚未产生，不再来自探索额度。正常验收最多一次，同一固定候选仅可针对具体技术失败再试一次，每次最多1,800秒、累计预留最多3,600秒。它不选择候选或经济参数，也不构成经济验证。没有信号可报告受限验收，不能编造交易。原生一致性验收、最终 observer 代码绑定和实际非评分输入→特征→意图记录全部通过，才允许开始前正式激活；如错过开始，保持未激活状态，不倒签、不静默顺延。

本次协议/登记工作 HTTP 请求 **0**、未来信号计算 **0**、新增原生回测 **0**、调度修改 **0**。这里只读取第三轮已经存在的真实产物并冻结条件。后续真实准备、自动唤醒和执行是否发生，必须分别留下可核验收据，不能拿协议文件存在或 heartbeat 启用代替。
