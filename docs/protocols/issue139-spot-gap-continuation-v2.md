# ISSUE139_SPOT_GAP_CONTINUATION_V2 — 待裁定缺失模型及一次续采

2026-09-08。原v1采集HEAD8c71959、终态HEADa057bd5保持不变，`BLOCKED_DATA`不改成PASS。当前新增网络、采集、native授权均0；此包仅合同和纯合成检查，不接采集器或交易引擎。原未提交spot139_model.py及其测试保留，不扩展。

## 证据等级与研究边界

监督提供的[官方Temporary System Maintenance Complete (2021/02/11)](https://www.binance.com/en/support/announcement/detail/aad7639a0ed9424bad585b508a61a433)线索是**官方域搜索索引正文**，含发布时间2021-02-11 04:38及05:00 UTC恢复交易声明；监督直接open重定向公告首页，未成功读到原公告页。本项监督消耗2search+1open，执行端追加0，不复制长正文。

05:00恢复与获准raw中05:00下一根open吻合，支持“与维护恢复相关”的解释；不证明03:40:54.773正是交易所停机开始，也不保证整页/所有资产历史完整。证据等级为 `OFFICIAL_INDEX_RESUMPTION_CORRELATION`，不是已认证完整停机日历。

绝对每小时连续门可能排除因市场不可交易而合法缺K线的历史，但它作为v1完整数据合同仍被正确触发。v2另外允许一个**探索性缺失观察模型**，不将源提升为完整或真实风险可认证。真实最大DD和停机期间可成交性UNKNOWN；训练结果至多SIMULATED_WITH_UNOBSERVED_INTERVALS，不是20%真实硬线通过、finalist或独立资格。

## 唯一例外边界与未知缺口停止

事前建议：每资产最多1个候选例外事件，只在2021-02-11 UTC `[03:00,05:00)` 两个小时槽内，最多2个受影响槽（短K线和缺K线合并计数）、包络最多2小时。该边界用于容纳已见异常并限制扩展，属于**暴露训练后制定的模型设计**，不是公告认证的精确停机开始，不能用作独立验证。两币均使用同一个边界，不根据收益分别放宽。

允许原生K线closeTime小于该小时末，但必须仍在本小时内；不改其open/high/low/close/volume。缺失槽保持缺失，不构造平价K线、零成交或OHLC。必须确认下一已获K线05:00存在且正常；跨页保留last-open与待确认例外状态，不能仅因一页结束就宣称恢复。若ETH无异常，原样完整通过；若同范围异常，只赋予同一探索性相关等级，不伪称公告单独认证ETH。

任何上述范围外短K线、缺口，或范围内超过2槽/恢复不在05:00：原始当前页保留并记 `QUARANTINE_UNKNOWN_GAP_STOP`，停止后续GET，不追查/追加网页、不扩大例外。同一资产重复/乱序、非整小时open、跨出小时close、坏OHLC/volume、身份/预算错误仍硬停止。未获页面的起止覆盖也必须验证，不能用允许缺口隐藏分页跳过。例外对整个source累计计，不逐页重置。

## 因果信号、库存、风险与恢复优先级

日线只有24个唯一且完整小时才能成为完整评分期日；短K线所在日和缺槽日均不完整。warmup的274日原生日线保留旧验证，不拿短日零填。当日是否完整只能到日终知道；不能把后来公告/将来的完整性标记提前喂给策略。

84日方向依赖按`t-84..t`连续85个完整日历日检查，ATR需要其完整20日true-range依赖；C需要完整273日波动依赖。缺一日则该项不可用，禁止新信号/新风险；不删日后对第84个“可用日”算收益。恢复到全部依赖可用后，才在新日线闭市决策、下一小时执行。前向信息绝不进入当前可用性判断。首次正信号可入场；止损/到期后仍需非正再转正；不因缺口偷偷解锁重入。

若已有持仓，缺口不造清仓、不借事后维护公告提前避险。持仓数量和已锁存风险状态保留；缺口估值为STALE，未观察价格变化为UNKNOWN，报告最后观察时间及不可观测时长。缺失ATR/vol只禁止增加风险，不凭缺值重新算更宽止损；原固定stop仍存在。停机中到期/已知退出意图排队，没有可交易open就无成交。

恢复时严格顺序：①以第一个实际恢复open重新估值，计入持仓跳空，更新可观测DD及10%/15%锁存；②已排队退出、到期、按恢复价触发的既定stop/必须减风险优先，仍受合法lot/cash约束；③只有实际成交后释放现金，不能用预计卖出收入；④不补做停机前未成交入场，等待新的完整日线决策。退出若受dust或规则限制不能完成，保留实际库存并阻止新风险。恢复成交仍按事前fee10/20bp及slip6/12bp，不能漏算gap或双扣费。

引擎不得自动fillna/forward-fill缺失K线后撮合。后续adapter必须用原始可交易时间白名单约束每个实际fill；任何无源open成交使整个模型失效，不能只在报表删掉。模型适配尚未授权执行/评分。样本过少可终止UNDERPOWERED；不缩短84日、不宣称只凭非负训练达到独立资格。

终点无下一可交易小时不造force-exit：现金、币量、按最后已观察价格的市值、该估值年龄、未实现损益和估算清仓成本分列；未实现不能伪装已实现，估算不是实际成交。DD只列可观察序列的最大值及未观察区间，20%真实风险结论UNKNOWN。

## 已获输入与不可重复请求

根固定 `/Users/shenjianpeng/.codex/runs/freqtrade-lab/issue139-spot-source-v1`；仅获准现货raw可读。此次再验4个响应SHA均匹配：

| raw | SHA256 | 用途 |
| --- | --- | --- |
| 001-exchangeInfo.json | 39b1adcf5d167250083ebb5eb7ad4dca59026c765989716dc4a0ceef58fe28e8 | 已获现货metadata；现用模拟规则非历史认证 |
| 002-klines.json | 593dc8eaf03b8e51fbc76f721eebc224985f999514e391c2700b7b4bcfc72347 | BTC274日warmup |
| 003-klines.json | 9cc2f33afbfc81bf279bf56c7e288bce442fe34455407e481f8c7e3221d2f045 | ETH274日warmup |
| 004-klines.json | 47c0851f4b2f38ce620c5c4d454c711d774cc68348fddffbd52380a8b891ae81 | BTC首1000行，保留短/缺异常 |

旧runtime terminal SHA `e357a2ef4a93b03d2053c79ca338838854d6293ce5e324c06a940b86f24d1a44`；旧budget SHA `c66ad06906688d5a946262ee0378d39e584931fbd4402edf002716967ce3145b`；登记后ledger SHA `8456411bc6020bc6ccb2d81e5b789d4f2926ad196cb8a5492bfee3caa9732020`。启动前须逐项再验证，任何漂移停止，不覆盖v1terminal/raw/登记。

## 有限续采建议，尚无授权

仍仅GET `https://api.binance.com/api/v3/klines`，symbol BTCUSDT/ETHUSDT，interval1h、limit1000、endTime1672531199999，无其他endpoint。先BTC，再ETH；不重复metadata/warmup/首BTC页。

BTC最后已获open是1613059200000（2021-02-11 16:00UTC），首续cursor **1613062800000（17:00UTC）**。之后cursor=实际返回末open+3600000；BTC剩名义16519小时，最多17页。ETH首cursor1609459200000（2021-01-01 00:00UTC），最多18页。总<=35GET。不能按固定行号推进游标或补17520行；合法例外导致总行数减少，报告原生行数和缺口清单。

正常页最多1000行且严格有序；首open须等于cursor，除非该cursor正处在唯一允许缺槽内并且随后05:00明确恢复。短页只有在剩余日历槽<=1000且最后open到达2022-12-31 23:00才算有界尾页完成；否则为不完整立即停止，不加试探尾页。BTC17/ETH18是上界，实际若提前完成也不凑请求。

新段在同root下唯一`continuation-v2`子目录，只有监督授权后才创建；编号005起，新累计budget保存旧4条及历史73条原样，v1budget原字节不改。继续持同父采集锁/登记锁，新的合同登记变更及允许post SHA必须绑定启动manifest；已存在续采状态永久拒绝重启，失败不换名/新root恢复。

旧spot已用4GET/290928bytes/3.4234702587s，原剩35GET/**16486288bytes**/**896.5765297413秒**。建议续段<=35GET、<=16486288bytes、活跃<=896秒、单响应<=1MiB/单请求<=20秒、间隔>=1秒、单worker、0retry/redirect。原spot累计<=39GET/16MiB/<900秒；全球最多112/122GET、31381145/67108864bytes、1065.1827094555/1800活跃秒。不是预算重置；失败/保留整响应收费照旧。

此上界在既定终点下可容纳分页，但不保证数据源完成：任意新未知异常立即终止。native预算仍0新增占槽，原最多10草案待后续固定实现审查。若续采成功，仅发布含缺口的探索性结构证据，不发布完整数据PASS或经济结论。

## 必要合成验证与下一步

纯检查草案 `lab/spot139_gap_proposal.py` 未连接collector/native；4项测试覆盖：原短/缺时间原样可见且不补行；未知缺口/重复/坏边界停止；84日不压缩且C依赖独立恢复；退出优先及不补入场。它不是完整执行adapter或已实现跨页续采器。现货模型未提交文件不改动。

监督只需裁定这个缺失模型/有限续采包；获准后再对现有薄collector做必要跨页状态和原始输入复用修改、冻结清单执行。当前没有新请求、档案下载、官方查询、PnL或策略参数选择。若不接受此探索性缺失模型，保留v1BLOCKED_DATA终态，不继续工程。
