# Issue139 fixed post-result attribution diagnostic V1

Authorization: https://github.com/He1met/freqtrade-lab/issues/139#issuecomment-5580156428

Freeze this script/manifest before the two new historical economic computations. This is a post-result explanatory design, not original preregistration, an independent window, native matching, or a risk-matched strategy comparison. Existing V3 B results and hourly logs remain immutable. No B replay, native initialization, GET, wallet engineering, weight search, or other candidate.

## Fixed mechanical benchmark

Initial 1000 USDT; BTC/USDT 400 USDT gross budget, ETH/USDT 400, remaining cash; one buy each at original 2021-01-01 01:00 UTC open. Same 39 frozen original sources, 2021-01-01 00:00 inclusive through 2023-01-01 00:00 exclusive, same metadata tick/lot/max/min/base commission precision. Price ceil(open*(1+slip),tick), gross quantity floor(min(400/price,maxqty),lot), received quantity minus ceil(gross*fee,basecommissionstep). Unspent budget stays cash. Base fee/slip .001/.0006; stress .002/.0012. No later buy, selection, rebalancing, timing, interest, drawdown halt or forced exit. Preserve terminal inventory, cost basis, mark-to-market and estimated hypothetical exit cost marked_value*(fee+slip) separately, matching B's estimate convention rather than claiming an actual sell or exact tick-rounded executable liquidation. A >20% observed drawdown remains a fact; benchmark is not admitted as a risk-compliant candidate. Current metadata and modeled fees retain prior historical-rule uncertainty.

Exactly one base and one stress benchmark; record reservation/success/failure in a separate analysis ledger. Two existing B NAV aggregations are also counted. Cash reference is analytically 1000→1000, zero interest/profit/DD. Posthoc largest-positive-period removal sensitivity: zero. Native budget remains 32 consumed +10 old sealed +6 Issue pending +48 unallocated =96; new diagnostic computations do not consume/reset that native ledger and are not concealed as zero economic computation.

## Calendar accounting and exposure

Hourly logs are post-action snapshots. A day's terminal NAV is previous hour cash/inventory marked at next day 00:00 prices BEFORE that timestamp's actions. Thus the midnight price move belongs to the preceding calendar day and the midnight fee to the following day. First NAV is initial 1000; require contiguous hourly logs and UTC whole-day bounds. Final 2023-01-01 00:00 has no source: carry last 2022-12-31 23:00 NAV for one hour and flag that boundary STALE. This is a disclosed terminal convention, not an observed midnight price. Missing source marks retain prior original open and mark age; any positive inventory with stale marks flags valuation. Report aggregate source gaps separately from held-inventory stale hours. Calendar month/year USDT changes sum daily changes; returns use each period's start NAV. Preserve original exit-month realized attribution with explicit label, never interpret as calendar market contribution.

B average/max total notional includes dust and uses marked inventory/NAV. Capital-time sums post-action notional across hourly intervals in USDT-hours; mean cash similarly, no interest. B active-market fraction uses existing active episode flags, excludes classified inactive dust, while total notional includes it. Benchmark uses any inventory. DD is maximum observed post-action hourly-open drawdown including initial 1000; missing/intrahour/terminal true DD remains UNKNOWN. No annualized exposure-adjusted return, regression alpha, or fitted risk-equivalent benchmark.

Entry point: `PYTHONDONTWRITEBYTECODE=1 uv run python scripts/analyze_spot139_attribution.py docs/issue139-attribution-v1-manifest.json --sha256 <frozen manifest sha>` checks bindings only. Add `--run` exactly once for authorized two computations. Existing fixed output directory refuses repeats or resets; no retry authorization. No historical data are embedded in tests; three synthetic checks cover calendar timing/telescoping, fixed purchases/rounding, and drawdown without intervention.

Runtime: Python >=3.10 required by the existing model dataclasses. The system python3 (3.9) import check failed before decoding or computation; use the repository uv interpreter. This is not a benchmark attempt.
